# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.

Pixel Perfect
Individuell inlämningsuppgift
Javascript ramverk

Välj en webbplats, vilken som helst och ta en screenshot någonstans på sidan. Din uppgift är sen att styla en react-applikation så den ser så lik ut som möjligt.

Välj ett eller kombinera flera sätt att använda css för att uppnå ditt mål. Välj mellan:

Tailwind
Design Bibliotek
JSS
Preprocessor (SASS/SCSS)

Applikationen behöver inte va responsiv. Målet är att få en applikation som är så lik ditt screenshot som du bara kan.

Inlämning:
Inlämning sker via Google classroom och du skall skicka in länk till repo samt
två screenshots, originalet och din version. Det går bra att lägga de båda bilderna direkt i projektmappen, men skriv då om det i Readme.

Presentation:
Kort presentation inför klassen där du pratar om vilket sätt du valt att styla på, vad du tyckte om det samt att du visar upp ditt resultat.

Kravspecifikation
G:
Git och github har använts
Du har inte använt “vanlig” css eller Css Modules endast
Du har stylat med något av ovan alternativ (eller kombinerat)
Du har en screenshot på en webbplats, samt på din version
Få din applikation så lik originalet som möjligt.
Minst 3 komponenter utöver App.tsx ska skapas och användas.

Lycka till!
